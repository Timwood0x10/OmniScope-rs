//! Data flow graph

/// Data flow graph for analysis
pub struct DataFlowGraph {
    // Placeholder
}

impl DataFlowGraph {
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for DataFlowGraph {
    fn default() -> Self {
        Self::new()
    }
}
